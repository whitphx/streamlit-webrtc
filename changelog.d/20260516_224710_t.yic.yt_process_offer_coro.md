### Chore

- Refactor `_process_offer_coro` in `streamlit_webrtc/webrtc.py`. Extract `_build_output_track` (uniform precedence: explicit source > processor-wrapped input > raw input > none) and `_notify_track_created` (the four-branch `kind`/`role` dispatcher) helpers. The function shrinks from ~225 lines to ~142 by collapsing the duplicated audio/video branches across SENDRECV / SENDONLY / RECVONLY into one helper call per mode. No public-API change; existing pytest layer-3 loopback coverage proves behavior preservation.
