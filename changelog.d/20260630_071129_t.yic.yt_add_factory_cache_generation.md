### Added

- Added an explicit `cache_generation` option to source and sink track factory
  helpers so apps can request fresh cached tracks across logical WebRTC
  sessions while preserving the default cache behavior.
