### Added

- Added `lifecycle_scope` support to source and sink track factory helpers.
  Factory-created tracks now default to the WebRTC session lifecycle and can
  opt into Streamlit-session persistence with
  `lifecycle_scope="streamlit-session"`.
