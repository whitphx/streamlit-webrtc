### Added

- Added `reset_key` support to source and sink track factory helpers, including
  a session-scoped default reset key, so apps can request fresh cached tracks
  across logical WebRTC sessions while preserving the default cache behavior.
